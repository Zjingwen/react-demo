import React, {
  Component,
} from 'react';
import Fieldset from '@Component/Fieldset';

class Hoc extends Component {
  render() {
    return (
      <Fieldset title='forwardRef-hoc-用法'>
        forwardRef-hoc-用法
      </Fieldset>
    );
  }
};

export default Hoc;
