import React, {
  Component,
} from 'react';
import Fieldset from '@Component/Fieldset';

class Memo extends Component {
  render() {
    return (
      <Fieldset title='memo'>
        memo
      </Fieldset>
    );
  }
};

export default Memo;
