import React, {
  Component,
} from 'react';
import Fieldset from '@Component/Fieldset';

class Index extends Component {
  render() {
    return (
      <Fieldset title='lazy'>
      lazy
      </Fieldset>
    );
  }
}

export default Index;
